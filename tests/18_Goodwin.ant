model Goodwin_oscillator()

  species X1, X2, X3   // mRNA, enzyme, product (repressor)

  // parameters (example values; keep positive)
  k1 = 5;   K = 2;   n = 3;   d1 = 1;
  k2 = 1.5; d2 = 1;
  k3 = 1.2; d3 = 1;

  // X1' = k1 / (1 + (X3/K)^n) - d1*X1
  X1' = k1 / (1 + (X3/K)^n) - d1*X1;
  X2' = k2*X1 - d2*X2;
  X3' = k3*X2 - d3*X3;

  // ICs (example)
  X1 = 1;  X2 = 1;  X3 = 1;
end
// @SIM T_START=0 T_END=20 N_STEPS=400
