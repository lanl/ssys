# BioModels benchmark suite requirements
# Install with: uv pip install -r requirements.txt

# Core dependencies (should already be installed with ssys)
# IMPORTANT: numpy<2.0 required for compatibility with libroadrunner
numpy>=1.24.0,<2.0
scipy>=1.11.0
sympy>=1.12
matplotlib>=3.7.0

# BioModels API access
bioservices>=1.11.2
requests>=2.31.0

# SBML to Antimony conversion (standalone - NOT tellurium)
antimony>=2.13.0
python-libsbml>=5.20

# Data handling
pandas>=2.0.0

# Jupyter notebook generation
nbformat>=5.9.0
jupyter>=1.0.0

# Progress bars for long operations
tqdm>=4.65.0

# Parallel processing utilities
joblib>=1.3.0

# JAX for fast numerical validation (autodiff)
# IMPORTANT: JAX >=0.4.26 requires numpy>=2.0, which breaks libroadrunner
jax>=0.4.0,<0.4.26
jaxlib>=0.4.0,<0.4.26
